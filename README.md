# Cache-cleaner
en: Cache Cleaner is a utility for cleaning the ~/.cache folder on Linux (Ubuntu-based) systems.  Usage: Run cache-cleaner in the terminal with any flag (use --help for assistance).  ru: Программа для очистки папки ~/.cache в Linux-системах (на базе Ubuntu/Debian).  Запуск: выполните в терминале cache-cleaner с любым флагом (--help для справки).
